{% extends 'base.html.twig' %}

{% block title %}Hello {{ mon_prenom }} !{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Chaaaalut {{ mon_prenom }}! ✅</h1>
      	<li class="nav-item active">
        	<a class="nav-link" href="{{ path('add_user')}}">S'inscrire <span class="sr-only">(current)</span></a>
      	</li>
        <li class="nav-item active">
        	<a class="nav-link" href="{{ path('add_article')}}">Ajouter un article <span class="sr-only">(current)</span></a>
      	</li>
      	<li class="nav-item active">
        	<a class="nav-link" href="{{ path('list_article')}}">Liste des articles <span class="sr-only">(current)</span></a>
      	</li>
</div>
{% endblock %}
